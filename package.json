{
  "name": "video-gallery",
  "version": "1.0.0",
  "description": "Static server for video gallery frontend",
  "scripts": {
    "proxy": "node proxy/index.js",
    "dev": "http-server ./static -p 8080"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
